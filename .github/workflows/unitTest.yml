name: Unit tests coverage

on:
  push:
    branches:
     - "develop"

job:
  build:
    runs-on: 
          ubuntu-latest
    strategy:
      matrix:
       node-version: [15.x]
    
    steps:
      - uses: actions/checkout@v1
      - run : npm install
      - run : npm run test


